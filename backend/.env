# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (Cambiar en producción)
JWT_SECRET=yupify_super_secret_key_change_in_production_123456789

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# HiFi API Configuration
DEFAULT_AUDIO_QUALITY=HI_RES_LOSSLESS

# Database (Si decides usar PostgreSQL/MongoDB en el futuro)
# DATABASE_URL=postgresql://user:password@localhost:5432/yupify
# MONGODB_URI=mongodb://localhost:27017/yupify

# Redis (Para caché en el futuro)
# REDIS_URL=redis://localhost:6379
